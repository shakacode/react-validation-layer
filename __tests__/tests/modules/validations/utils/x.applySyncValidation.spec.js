/* @flow */

// import { applySyncValidation } from '../../../../../src/modules/validations/utils';

describe('modules.validation.utils.applySyncValidation()', () => {
  xit('add tests', () => {});
});
