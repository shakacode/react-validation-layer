/* @flow */

// eslint-disable-next-line max-len
// import { buildCompleteSyncValidationResults } from '../../../../../src/modules/validations/utils';

describe('modules.validation.utils.buildCompleteSyncValidationResults()', () => {
  xit('add tests', () => {});
});
