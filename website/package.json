{
  "name": "react-validation-layer-website",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router-dom": "4.0.0-beta.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp CNAME build/",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "yarn run build",
    "deploy": "surge build/",
    "eject": "react-scripts eject"
  }
}
